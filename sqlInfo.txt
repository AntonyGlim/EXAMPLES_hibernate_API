/*Перед началом следует запустить Postgrese (можно портабельный) с правами администратора */
/*У учетной записи должен быть установлен пароль 'admin'*/

/*Выставляем кодировку для Windows*/
\! chcp 1251

/*Создаем базу hiber*/
DROP SCHEMA IF EXISTS hiber CASCADE;
CREATE SCHEMA hiber;

/*Переходим в выбранную схему*/
SET search_path TO hiber;

/*Список таблиц (таблицы все созданы автоматически)*/
\dt

/*Список схем*/
\dn